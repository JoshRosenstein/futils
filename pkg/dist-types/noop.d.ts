/**
 * A function that returns always `undefined`.
 * @signature
 *    F.noop()
 * @example
 *    onSomething(F.noop) // => void
 * @category Function
 */
export declare const noop: () => void;
export default noop;
